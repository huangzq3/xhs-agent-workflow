# 版本变化

本文件记录发行包版本。核心机器数据契约使用独立的 Schema 版本，二者不要求同号；关系见[架构说明](docs/ARCHITECTURE.md#版本维度)。

## 2.6.0 — 2026-08-25

### AI Agent 原生生图单一路径

- 删除基于 Pillow 的文字卡、叠字和拼图脚本，移除 Pillow 依赖及安装器能力探测；工作流不再包含本地栅格生图后备路径。
- 生图只交给当前 AI Agent 已展示、已授权并记录 `capability_id` 的原生能力；能力缺失时仅交付 prompt、布局规格、未执行任务或人工素材任务。
- `image_job` 升级为 1.1.0：完成任务必须记录 Agent 结果引用，并只登记导出文件的图片签名、媒体类型、字节数与 SHA-256，不在本地解码、裁剪或核验像素比例。
- 图片与视频素材由清单脚本登记文件事实，图片内容、构图和比例改由 Agent 视觉检查；最终是否进入稿件仍由素材权利检查和 G3 人工门禁决定。
- 保留 `image_job` 1.0.0 已完成记录的只读校验兼容性。

### 验证

- 新增无 Pillow、无本地栅格脚本和 1.0.0 兼容性测试；Python 3.9 与 3.14 使用同一测试路径，不再因图片依赖跳过用例。

## 2.5.0 — 2026-08-25

### 定位生长闭环

- 把账号定位从一次性完整定义改为带稳定核心、开放问题、反向边界、适用范围和复核时间的搜索过程。
- 每项定位假设新增组成部分、可观察推论、反驳信号、成熟度、把握度、正反证据和复核时间。
- 选题与稿件新增相同的 `positioning_trace`，锁定 Persona revision、内容支柱、受众任务、交付价值、证据职责、记忆资产和定位假设；G2/G3 阻断静默漂移。
- 复盘逐项回应稿件绑定的定位假设，并显式记录受众共鸣、内容兑现、平台分发、创作者适配、业务适配和受众市场镜像。
- `mode=validated` 改为“当前声明范围内稳定”的兼容摘要；G1 反查上一版定位、真实复盘、至少两篇有效定稿内容、数据快照、四类关键证据流、反向证据和市场镜像。
- 实验区分早期 `exploration_probe` 与后期 `controlled_optimization`，定位修订建议必须关联复盘已经评估的假设并声明账号表达迁移动作。
- 非首版 persona 新增修订谱系门禁：必须回指已确认上一版，并由后续已确认战略，或真实复盘与已批准 G6 实验提供修订依据。

### 兼容性与验证

- 核心 Schema 升级为 2.4.0；历史 2.2.0 与 2.3.0 artifact 仍可读取和校验。
- Article Audit 接入信封新增 2.4.0 支持，独立审计语义版本仍为 1.0.0。
- 核心测试新增定位证据引用、选题—稿件追踪、无效方向性结论、复盘—实验—Persona 修订谱系和跨内容稳定化门禁场景。
- G6 新增复盘—实验引用校验，下一轮实验不得引用关联复盘未评估的定位假设。

## 2.4.0 — 2026-08-25

### 新增

- 在 `account_strategy` 中加入创作者方向：90 天主目标、商业去向与受众匹配、当下和未来价值、关系姿态、信任引擎、内容引擎、记忆资产、红线、证据、假设和未知。
- persona 新增定位问题诊断，区分初次建立、定位基础不清、内容兑现不稳定、受众错配和证据不足。
- persona 新增创作者方向的账号投射：账号角色、价值、关系、信任、内容引擎、记忆资产和商业连接。
- 内容支柱显式映射受众任务、交付价值、证据职责、商业连接和定位假设。

### 兼容性

- 核心 Schema 升级为 2.3.0。
- 新建账号策略与 persona 必须包含方向、诊断和映射字段。
- 历史 Schema 2.2.0 artifact 仍可读取与校验；新修订按 2.3.0 生成并重新确认。

### 文档

- README 改为结果、证据和快速开始优先的公开项目入口。
- 版本史、架构、开发命令、支持范围与隐私边界拆分为独立文档。
- README 演示改为由当前 Schema 有效示例实际渲染，旧 V2.2.x 审计截图不再作为当前版本证明。

## 2.3.0

### 新增

- 通用、独立的文章审计门禁：写作者与审计者使用不同身份、全新上下文和只读审计权限。
- 人工审阅与审计统一交付中文 HTML，不向内容负责人展示原始 JSON 或内部状态码。
- 高风险内容缺少不同模型复核时，不得得到无条件通过结论。
- 支持平台原生定时、运行工具到点唤醒和人工到点交接三种排期方式。
- 短期和长尾复盘统一从平台确认的实际上线时间开始计算。

### 安全与兼容性

- 稿件、审计记录或定稿确认任一变化，原有发布资格失效。
- 定时发布记录包含时区与最晚允许执行时间；超过期限不自动补发。
- 内部机器契约继续以向后兼容方式演进。

## 2.2.2

- 建立发布与复盘时间闭环。
- 区分计划发布时间、排期状态与平台确认的实际上线时间。
- 增加发布防重、结果未知时停止自动重试、短期与长尾复盘门禁。
