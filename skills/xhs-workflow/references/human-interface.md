# 人工交互与审阅规范

本文件约束所有面向内容负责人的提问、状态说明、审批请求和报告。机器字段、内部状态码与命令参数仍可在内部文件和工具调用中使用，但不得原样变成人工界面。

## 不可违反的规则

1. 跟随内容负责人当前使用的语言；无法判断时使用简体中文。
2. 提问先说明业务背景、当前结论和影响，再提出一个可直接回答的问题。
3. 不要求内容负责人理解内部阶段号、字段名、状态码、文件结构、哈希或命令参数。
4. 不在对话、审批请求或交付说明中粘贴原始 JSON、NDJSON、Schema 或命令输出。
5. 人工审阅只交付安全转义的 HTML 页面；不把 Markdown 或 JSON 当作人工报告。
6. 每次人工确认必须提供“确认通过、退回修改、暂不处理”三种可理解的选择。沉默不算确认。
7. 如内容被改写、图片被替换、发布账号变化或数据范围变化，用业务语言说明原确认已失效以及需要重新确认的内容。
8. 只有内容负责人明确要求“查看技术数据”时，才可另行提供机器文件；该文件不得替代 HTML 审阅页。

## 人工阶段名称

以下左列只供执行器识别，右列才可出现在人工提问和报告中。

| 内部代码 | 人工界面名称 |
|---|---|
| G0 | 启动与授权确认 |
| 账号策略上的 G1 | 账号运营策略确认 |
| 账号定位上的 G1 | 账号定位确认 |
| G2 | 选题确认 |
| G3 | 内容定稿确认 |
| G4 | 发布前确认 |
| G5 | 数据采集范围确认 |
| G6 | 迭代实验确认 |

常见内部值也必须翻译：

| 内部值 | 人工界面表达 |
|---|---|
| persona / `mode=assumed` | 账号定位 / 定位探索中（待验证） |
| `mode=validated` | 当前声明范围内已稳定 |
| `exploration` / `consolidation` / `stable_within_scope` / `reopening` | 探索定位空间 / 阶段性收敛 / 当前范围内稳定 / 重新检验定位 |
| `review_required` | 待人工确认 |
| `approved` | 已确认 |
| `unknown`（发布状态） | 发布结果待核对 |
| `needs_human` | 需要人工判断 |
| `blocked` | 当前规则不允许 |
| `full` / `assisted` / `document_only` | 完整执行 / 人机协作执行 / 仅生成方案 |
| `platform_native` / `agent_wakeup` / `manual_handoff` | 使用平台原生定时发布 / 由当前运行工具到点唤醒执行 / 由账号负责人到点手动发布 |
| `published_at` | 实际上线时间 |
| `passed` / `audit_failed` / `human_decision_required` | 审计通过 / 审计未通过 / 需要内容负责人决定 |
| `P0` / `P1` / `P2` | 必须先解决 / 定稿前应解决 / 优化建议 |
| `initial_definition` / `foundation_unclear` / `delivery_inconsistent` / `wrong_audience` / `evidence_insufficient` | 首次建立 / 定位基础不清 / 内容兑现不稳定 / 受众错配 / 证据不足 |
| `create` / `revise` / `keep` / `test_first` | 建立定位搜索空间 / 修订定位 / 保持当前定位 / 先补充验证 |

## 提问模板

采用以下顺序：

1. **发生了什么**：一句话说明当前阶段与已完成内容。
2. **需要关注什么**：列出最多五项真正影响决定的信息。
3. **有什么风险或缺口**：明确缺失证据、外部数据处理、版权或发布不确定性。
4. **需要作出什么决定**：只提出当前能回答的一个问题。

错误示例：

> persona=assumed，是否批准 G1？

正确示例：

> 当前账号仍在探索定位。这一版先固定真实能力和表达边界，同时保留两个开放问题，并用不同内容验证受众和价值假设。内容负责人是否同意按这个搜索空间开始验证？这不是确认定位已经正确，也可以退回修改或暂不处理。

错误示例：

> policy_check=needs_human，请批准 G4。

正确示例：

> 发布前检查发现同类选题距离上次发布不足账号设定的间隔。内容负责人是否仍要发布本篇？如继续，本次例外只授权一次发布尝试。

## HTML 审阅页

人工审阅页至少包含：

- 当前业务阶段和状态；
- 本次需要决定的事项；
- 与决定直接相关的内容、证据、风险和局限；
- 信息来源；
- 历史人工决定；
- 默认折叠的追溯信息。

内容定稿页还必须同时展示独立文章审计的结论、风险级别、尚未解决的问题与修订方向。不展示内部英文枚举或原始问题 JSON。审计结论为“需要内容负责人决定”时，页面要求记录具体取舍理由。

账号运营策略页必须单独展示“创作者方向”，包括 90 天主目标、商业去向、当下/未来价值、关系与信任机制、内容引擎、记忆资产、证据、假设和未知。账号定位页必须先展示“定位问题判断”，再展示“创作者方向在本账号的落地”“定位生长状态”“稳定核心与开放问题”和逐项假设。复盘页必须展示五路定位证据和受众市场镜像。不得将内部诊断或成熟度枚举直接暴露给内容负责人。

页面不得包含原始 JSON、代码块、内部字段表、英文状态机或未经翻译的阶段号。关联编号和校验指纹只能放在折叠的“追溯信息”区域。

使用核心辅助器生成单项审阅页：

```bash
python3 scripts/workflow_cli.py render <内部数据文件> --output <审阅页.html>
```

使用以下命令生成整轮或整账号的人工审计报告：

```bash
python3 scripts/workflow_cli.py audit-report --root <工作区> --output <人工审计报告.html>
```

执行器生成 HTML 后，应在最终回复中只提供 HTML 路径和一句业务摘要，不附带机器数据文件。
