---
name: xhs-content-review
description: 从平台确认的实际上线时间起算小红书内容的短期与长尾周期，采集并分离流量、信任和人工评价指标，以相近生命周期和账号自身基线生成复盘，诊断定位基础、内容兑现、受众匹配与证据充足度，并提出需要人工确认的创作者方向、账号阶段或定位修订。处理笔记复盘、长尾复盘、信任指标、内容数据分析、阶段判断、定位问题诊断和观察窗口比较时使用。
---

# 小红书内容复盘

把原始快照、计算指标和解释分层保存在内部机器文件中。内容负责人只接收中文可视化 HTML；不得在对话中输出原始 JSON。遵守 [人工交互与审阅规范](../xhs-workflow/references/human-interface.md)。

## 前置条件

1. 内部要求明确的本轮任务、已发布记录、已定稿内容和该内容明确引用的 persona；定位判断遵守 [定位生长闭环](../xhs-workflow/references/positioning-loop.md)。
2. 核对账号、内容与平台内容记录一致。
3. 核对发布记录中的“实际上线时间”及其依据。计划发布时间、排期提交时间、运行工具开始执行时间和发现页面的时间都不能自动替代实际上线时间；无法核对时暂停复盘排期并请求账号负责人确认。
4. 在采集前请账号负责人完成“数据采集范围确认”，明确从实际上线时间开始计算的短期窗口、信任指标、长尾时间点、人工评价标准、个人数据和同步目标。详见 [references/measurement-and-long-tail.md](references/measurement-and-long-tail.md)。
5. 读取 `metrics_collection` 能力快照，只使用已展示的能力。不可用时接受结构化 JSON/CSV 手工导入；不得编造缺失值。

## 采集

1. 以上线时间为零点生成每个观察窗口的到期时间。未满周期时不提前生成该周期的正式快照；延期采集时同时展示应采集时间和实际采集时间。
2. 按观察窗口创建不可变 `metrics_snapshot`，区分 `initial` 和 `long_tail`，保存实际上线时间锚点、观察起止时间和已观察时长，不覆盖历史快照。
3. 分开保存存量指标、期间增量和派生指标。
4. 图文、视频和文本使用不同指标集合，见 [references/data-analysis.md](references/data-analysis.md)。
5. 分母为零或字段不可用时写 `null` 并登记 `missing_fields`。
6. 信任指标单独写入 `trust_metrics`；需要语义判断的评论质量写入 human-assessed `qualitative_metrics`。
7. 不采集未经“数据采集范围确认”允许的受众画像或个人信息。

## 分析

1. 只比较上线后观察时长相近的样本；首选同账号、同内容形式、相近生命周期和相近周期的历史分位数作为基线。
2. 样本不足时明确标为 exploratory，不使用固定阈值伪装确定结论。
3. 把输出拆成：
   - observation：数据直接显示的现象；
   - hypothesis：可能原因；
   - alternative_explanations：其他解释；
   - diagnosis：基于证据的暂定判断；
   - intervention：可验证行动。
4. 不把相关性写成因果关系。
5. 生成内部复盘记录，使用 `time_context` 汇总实际上线时间、时间依据、每个周期的应采集时间、实际采集时间、已观察时长和对应快照；同时记录基线、信任与长尾观察，并分别填写账号阶段复核和定位验证建议。
6. 对内容 `positioning_trace` 中的每个定位假设分别写入结果。结果必须引用本篇内容与本次快照，并区分 `supported`、`refuted`、`inconclusive` 和 `not_tested`；不得遗漏，也不得评价本篇没有承担的定位假设。
7. 先独立判断内容兑现与有效曝光。内容没有完成受众任务、价值或证据职责，或平台没有提供足以判断的合格曝光时，只能无结论，不能支持或推翻定位。
8. 显式记录五路证据：受众共鸣、内容兑现、平台分发、创作者适配和业务适配。缺失或不适用也必须记录，不能因平台指标较好而省略创作者与业务侧证据。
9. 记录 `market_mirror`：受众如何描述账号、为何关注、是否出现合格需求、错误期待或无反应。它与平台总量指标分开，不把当前被分发到的人直接解释成完整潜在受众。
10. 对定位验证再区分：定位基础是否错误、内容是否未稳定兑现既定价值、实际受众是否错配、证据是否尚不足。同时评估当下/未来价值、信任引擎、内容引擎和主记忆资产是否被内容实际兑现。
11. 阶段、画像或创作者方向变化只作为需要人工确认的修订建议，不能直接改写 account_strategy 或 persona。证据不足或仅是内容兑现不稳定时，不建议修订定位或创作者方向。
12. 从内部复盘记录生成中文 HTML 报告；需要飞书时按 [references/feishu-base-schema.md](references/feishu-base-schema.md) 同步，且不得对每日累计数据求和。

## 输出

- 机器层：一个或多个数据快照和一份复盘记录，不直接交付给内容负责人。
- 人工层：中文 HTML 复盘报告，突出每项定位假设的结果、五路证据、受众市场镜像、其他可能解释、风险、局限和下一步决定。
- 可选同步层：飞书视图；不得把同步层当作机器事实源。

展示规范见 [references/report-design.md](references/report-design.md)。HTML 生成失败时应明确报告交付失败，不得用原始 JSON 顶替人工报告。
