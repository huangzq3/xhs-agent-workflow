# Topic report Markdown 视图

本文件不再定义机器交接格式。使用 xhs-workflow/scripts/workflow_cli.py render 从 topic_report JSON 生成 Markdown。

建议的人读顺序：

1. 范围、账号、时间与数据来源
2. 核心局限
3. 已选择和待选择的候选
4. 每个候选的受众需求、证据、评分、置信度、反证与风险
5. G2 决定区

不得在 Markdown 中加入 JSON 不存在的评论、评分或结论。
